# Model Weights

The YOLO model weights (`.pt`) and the exported ONNX model (`.onnx`) used in this repository are automatically downloaded and generated using the following script:

```sh
cd py_server
uv run utils/get_model.py
```
